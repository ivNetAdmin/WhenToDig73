<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Wtd.Core.Views.MainPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddOrUpdateJobCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <ListView 
                ItemsSource="{Binding JobList}" 
                ItemTapped="OnItemTapped" 
                ItemSelected="OnItemSelected"
                x:Name="JobListView" RowHeight="45">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Date.LocalDateTime,StringFormat='{0:dd/MM}'}" VerticalOptions="Center"/>
                                <Image HeightRequest="20" Source="{Binding Path=TypeImage}" VerticalOptions="Center"/>
                                <Label Text="{Binding Description}" VerticalOptions="Center"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>